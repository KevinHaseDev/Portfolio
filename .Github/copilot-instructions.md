use Angular CLI 20+ standards and best practices.
write javascript/typescript function names in english and with clear meaning 
write javascript/typescript variable names in english and with clear meaning 
Follow clean code principles (clear naming, small functions max 14 lines).
use innerHTML instead of eventlistener 
use let instead of const 
never use var 
never use inline styles 
use template literals instead of string concatenation 
use flexbox instead of grid 
use semantic html tags 
one page dont have more denn 400 lines of code. 
Always use async/await for asynchronous code instead of .then(). 


